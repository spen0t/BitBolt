@model IEnumerable<BitBolt.Models.Termekek>

@{
    ViewData["Title"] = "Termékek";
}


<div class="container">
    <div class="row">
         <div class="col-lg">
<h1>Termékek</h1>

<p>
    <a class="link-dark" asp-action="Create">Új termék felvétele</a>
</p>

</div>

<div class="col-sm">
        <label for="Keresettszo" class="control-label"></label>
        </div>

<div class="col-sm">
<div class="col-sm-4 col-md-6 col-lg-10">
<form asp-action="KeresesEredmeny">
    <div class="form-group">

        
        <?php
        //connect to mysql database
        $connection = mysqli_connect("localhost","asztrik","Szeretemafagyit22'","BitBolt_db") or die("Error " . mysqli_error($connection));

        //fetch data from database
        $sql = "select * from Termekek";
        $result = mysqli_query($connection, $sql) or die("Error " . mysqli_error($connection));
        ?>

        <input name="KeresettSzo" autocomplete="off" class="form-control" list="kereso" placeholder="Keresés"/>
        
    </div>

    <div class="mt-3">
    <div class="form-group">
        <input type="submit" value="Keresés" class="btn btn-dark" />
    </div>
    </div>
</form>
</div>
</div>
</div>
</div>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Megnevezés)
            </th>
            <th>
                Gyártó
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Típus)
            </th>
            <th>
                Beszerzési ár
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Megnevezés)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Gyáró)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Típus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BeszerzésiÁr)
            </td>
            <td>
                <a class="link-dark" asp-action="Edit" asp-route-id="@item.Id">Szerkesztés</a>  / 
                <a class="link-dark" asp-action="Delete" asp-route-id="@item.Id">Törölés</a>
            </td>
        </tr>
}
    </tbody>
</table>
